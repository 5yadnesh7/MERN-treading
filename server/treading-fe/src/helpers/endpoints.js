import { backEndUrl } from "./constants";

export const endpoint = {
    baseUrl: backEndUrl,
    nifty: {
        todaysData: "/niftyData",
    },
    bankNifty: {
        todaysData: "/bankNiftyData",
    },
}